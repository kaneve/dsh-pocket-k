vendored-from: mexiaosqwq/dsh-web-mobile (upstream)
upstream-commit: 7ae9bcbe006afbb04fb4e653e55e8771c1749518 (main, v2.4.1)
merge-base: e1910533484bed85dd5afbbb8dd1206de3b32132 (v2.0.0 — the previous vendored fork's base)
version: 2.4.1
date: 2026-09-15
upstream: mexiaosqwq/dsh-web-mobile (MIT)

how: 3-way merge — base = upstream v2.0.0, ours = the previously vendored local
     fork (2.0.0 + the deviations below), theirs = upstream v2.4.1. Upstream's
     rename dsh-mobile-nav-* -> dsh-web-mobile-* was ADOPTED (the new modules and
     the merged CSS both use the new names; keeping the old names on the JS side
     made those selectors silently no-op). New upstream modules pulled in:
     effects/sidebar-swipe, effects/session-menu, effects/subagent-chip-touch,
     effects/gesture-guard, effects/composer-keyboard-guard,
     effects/file-viewer-compat, core/raf-scheduler.

local-deviations:
  - PR#42: styles/layout.css.ts + styles/base.css.ts — mobile drawer/backdrop z-index raised above third-party overlay z500 (drawer 600, backdrop 590)
  - #48: effects/aionui-compat.ts + styles/compat.css.ts — hide files/explorer entries when host has no aionui explorer; Files closes the drawer
  - t17: effects/stats-line.ts — compact session stats labels (LLM→模型, 工具调用→工具, TTFT removed, tok/s→t/s, 缓存命中→缓中, 输入→入, 输出→出, tok→t), remove TTFT-only groups; styles/compat.css.ts — stats strip font-size 12→11px, tighter gap/padding, horizontal scroll retained
  - phone-chrome: [class*="usg_"] kept in the drawer-close selector list (upstream's tap-path refactor dropped that token)
